# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

This repository is currently empty scaffolding. It contains only a `README.md`
(`# dukani-app-V2`) and no application code, package manifest, build
configuration, or tests yet.

There is no build, lint, or test tooling to run, and no architecture to
document, because no source code has been added.

When code is introduced to this repository, update this file with:
- Build, lint, and test commands (including how to run a single test)
- The project's high-level architecture and directory structure
- Key conventions specific to this codebase
